# Core dependencies for Hypermedia package

# Database ORM
sqlalchemy>=2.0.0
alembic>=1.12.0

# Cryptography and hashing
# BLAKE2b is built into Python's hashlib

# Media metadata extraction
Pillow>=10.0.0  # Image processing and EXIF
mutagen>=1.47.0  # Audio metadata (ID3, Vorbis, etc.)

# File type detection
python-magic>=0.4.27

# Data validation
pydantic>=2.0.0

# Utilities
python-dateutil>=2.8.2
